# network

Minimal custom-mode VPC with explicitly-declared subnetworks and private Google access enabled.

This is a **local module** consumed from `env/**` via a `${get_repo_root()}/modules/network`
source (see `env/_envcommon/network.hcl`).

<!-- BEGIN_TF_DOCS -->
<!-- Run `terraform-docs .` to populate this section. -->
<!-- END_TF_DOCS -->
